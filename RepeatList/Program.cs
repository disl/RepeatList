using RepeatList;

namespace MauiApp
{
    public static class Program
    {
        public static void Main(string[] args)
        {
            var app = MauiProgram.CreateMauiApp();
            app.Run(args);
        }
    }
}